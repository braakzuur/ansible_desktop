---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install packages
    package:
      name:
        - htop
        - rustdesk
        - bitwarden
        - signal
        - brave
        - obsidian
